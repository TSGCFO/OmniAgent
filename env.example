# OmniAgent Environment Configuration

# Required: OpenAI API Key for LLM operations
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Custom OpenAI endpoint (leave blank to use default)
OPENAI_BASE_URL=

# Required: PostgreSQL Database URL for memory storage
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://localhost:5432/omniagent

# Optional: Slack Integration (if using Slack triggers)
SLACK_TOKEN=your_slack_bot_token_here
SLACK_SIGNING_SECRET=your_slack_signing_secret_here

# Optional: Node Environment
NODE_ENV=development

# Optional: Mastra Dev Server
MASTRA_BASE_URL=http://localhost:4111

# Optional: Inngest Configuration (if using Inngest workflows)
INNGEST_EVENT_KEY=your_inngest_event_key_here
INNGEST_SIGNING_KEY=your_inngest_signing_key_here

# Optional: Additional AI Provider Keys (if needed)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
# GROQ_API_KEY=your_groq_api_key_here

# Optional: MCP Server URLs (if using external MCP servers)
# MCP_SERVER_URL=http://localhost:3000
